# Production / preview: full URL of the deployed API (no trailing slash).
# Leave unset for local dev — Vite proxies /api → http://localhost:4000
# In Vercel (frontend project), set this for BOTH Production and Preview — beta URLs need it too.
VITE_API_URL=https://smartseason-m3hv.vercel.app
